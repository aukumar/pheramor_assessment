<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="project.assessment.pheramor.com.assesment_project">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
         >
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PasswordActivity"></activity>
        <activity android:name=".NameActivity"></activity>
        <activity android:name=".DOBActivity"></activity>
        <activity android:name=".InterestActivity"></activity>
        <activity android:name=".OptionalActivity"></activity>
        <activity android:name=".ProfilePictureActivity"></activity>
        <activity android:name=".SummaryActivity"></activity>

    </application>

</manifest>